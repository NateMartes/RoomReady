
class PromptParser:
    def __init__(self, prompt):
        self.prompt = prompt

    def get_summary(self):



class RiskObject:
    def __init__(self, prompt):
        self.prompt = prompt

if __name__ == "__main__":
    with open(exampleprompt)
    PromptParser(prompt)
    response = {
        "summary": "",
        #"total_risks": "",  # Bytes length of the file
        "risks" : []
    }

    summary = Prompt.get_summary()
    risks = Prompt.get_risks()
    for risk in risks:
        riskname = risk[0]
        riskdesc = risk[1]
        riskimprov = risk[3]

   # Add JSON entry
